import style from 'styles/topLocation.module.scss'

interface TopLocationProps {}

const TopLocation = ({}: TopLocationProps) => {
    return <div className={style.container}></div>
}

export default TopLocation
